export interface Frag {
    Brand?: string,
    Description?: string,
    Gender?: string,
    Id?: string,
    ImageUrl?: string,
    Name: string,
}

export interface UserInfo{
    admin: 0,
    email: string,
    photoUrl: string, 
    uid: string,
}